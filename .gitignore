__pycache__
generated

# Claude Code — machine-local permissions (project skills under .claude/skills/
# ARE tracked); this file is per-machine and should not be committed.
.claude/settings.local.json
characters
characters_pinyin_backup
practice
*.csv
*.xlsx
.venv

# Character Studio build pipeline — large external download (regenerate via curl
# in tools/build/fetch_chars.py); intermediate caches.
tools/build/dictionary.txt
tools/build/*.txt
tools/build/common_pool.json

# 史案 content pipeline — transient ingest artifacts (regenerable; data.js is the build output)
history-reader/build/input/*.residue.md
history-reader/build/input/*.skel.json

# backend (Cloudflare Worker)
backend/node_modules/
backend/.wrangler/
