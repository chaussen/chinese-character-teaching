# Python dependencies for the remaining Python tool (Kahoot processor).
#
# The card maker is now a TypeScript project — see cardmaker/ (no Python needed).
#
#   pip install -r requirements.txt

XlsxWriter        # kahoot: write .xlsx question sheets
pinyin            # kahoot: cedict translations
